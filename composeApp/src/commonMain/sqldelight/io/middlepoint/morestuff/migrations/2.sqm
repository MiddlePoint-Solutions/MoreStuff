import io.middlepoint.morestuff.shared.domain.enums.ScheduleType;
import kotlin.Boolean;

CREATE TABLE schedule_migration (
        id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
        task_id INTEGER NOT NULL,
        create_time TEXT NOT NULL,
        schedule_time_local TEXT,
        schedule_time_utc TEXT,
        timezone TEXT NOT NULL,
        active INTEGER AS Boolean NOT NULL DEFAULT 1,
        schedule_type TEXT AS ScheduleType NOT NULL,
        FOREIGN KEY(task_id) REFERENCES task(id)
);

INSERT INTO schedule_migration SELECT *, "OneTime" AS schedule_type FROM schedule;
DROP TABLE schedule;
ALTER TABLE schedule_migration RENAME TO schedule;